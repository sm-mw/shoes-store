package org.sm.mw.order;

import org.sm.mw.order.delivery.Deliverable;

public class OrderService {

    // todo finish or delete
    public void chooseDeliveryProvider(Deliverable deliverable) {
//        deliverable.applyDelivery(DeliveryFactory.resolve(deliverable));
    }
}
